import React, { Fragment } from 'react';

const About = (props) => {

    return(
        <Fragment>
            <p>This is a fun site for safe media useage.</p>
        </Fragment>
        );
};

export default About;